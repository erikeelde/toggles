name: "Setup android"
description: "Setup android and java to run in a workflow"

inputs:
  cache-cleanup:
    required: false
    default: "true"
    description: "Removing unused files from Gradle User Home before saving to cache"

runs:
  using: "composite"
  steps:
    - name: "Setup java"
      uses: ./.github/actions/setup_java_21

    - name: "Setup gradle"
      uses: ./.github/actions/setup_gradle
      with:
        cache-cleanup: ${{ inputs.cache-cleanup }}

    - name: "Setup Android"
      uses: ./.github/actions/setup_android
